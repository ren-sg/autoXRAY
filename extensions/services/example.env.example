# Скопировать на сервере:
#   cp extensions/services/example.env.example extensions/services/my-service.env
# и отредактировать. Файл *.env в git не попадает.

SERVICE_NAME=my-service
SERVICE_DOMAIN=app.example.com
SERVICE_UPSTREAM=http://127.0.0.1:8080
SERVICE_WEBSOCKET=0
